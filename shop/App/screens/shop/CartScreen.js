import React from "react";
import {
  ScrollView,
  View,
  Text,
  Image,
  Button,
  StyleSheet,
  FaltList,
} from "react-native";

const CartScreen = (props) => {
  return (
    <View>
      <Text>The Cart details</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
export default CartScreen;
